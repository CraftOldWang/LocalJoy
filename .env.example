# Optional docker-compose.local.yml overlay.
# The advertised broker address must resolve and be reachable from BOTH Java and Canal.
# Docker Desktop default; on another host, set a reachable host IP or DNS name.
ROCKETMQ_BROKER_IP=host.docker.internal
