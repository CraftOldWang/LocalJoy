# 趣享生活项目面试问题清单

> 项目：趣享生活 - 本地生活服务平台<br>
> 技术栈：Spring Boot、MySQL、Redis、RocketMQ、MyBatis-Plus、Canal<br>
> 用法建议：先按“项目功能点”逐项准备，再按“面试官视角”模拟连环追问。每个问题下面的“追问”通常才是面试真正区分深度的地方。

## 一、按项目功能点拆解

### 1. 项目整体介绍与业务架构

1. 这个项目主要解决什么业务问题？你在里面负责了哪些核心模块？
   - 追问：哪些功能是你独立设计实现的？哪些是基于课程或已有代码改造的？
   - 追问：如果让你用 1 分钟介绍这个项目，你会怎么讲？

2. 你的项目为什么适合用 Redis、RocketMQ、Canal 这些中间件？
   - 追问：如果不用这些中间件，只用 MySQL 能不能做？瓶颈在哪里？
   - 追问：这些组件分别解决了性能、并发、一致性里的哪个问题？

3. 项目中最核心的高并发链路是哪一条？请求从前端到落库的完整流程是什么？
   - 追问：哪一步是同步返回给用户的？哪一步是异步执行的？
   - 追问：如果异步链路失败，用户已经拿到 orderId 了怎么办？

4. 这个项目的主要数据表有哪些？它们之间是什么关系？
   - 追问：秒杀库存表和订单表之间如何保证一致？
   - 追问：为什么要单独设计秒杀商品或秒杀券表，而不是直接扣商品表库存？

5. 你项目中有哪些地方用到了 Redis？分别使用了哪些数据结构？
   - 追问：String、Set、ZSet、Geo、Hash 分别适合什么场景？
   - 追问：哪些 Redis key 需要设置 TTL？哪些不适合设置 TTL？

6. 项目中哪些地方用了异步化？为什么这些地方适合异步？
   - 追问：异步后会引入哪些新问题？
   - 追问：如何向用户解释“下单成功”和“订单最终创建成功”之间的区别？

7. 这个项目的性能瓶颈可能在哪里？
   - 追问：Redis、MySQL、MQ、应用线程池分别可能成为瓶颈的场景是什么？
   - 追问：如果 TPS 从 2000 提升到 2 万，你优先改哪里？

8. 项目里有哪些一致性问题？你分别怎么解决？
   - 追问：Redis 库存和 MySQL 库存不一致怎么办？
   - 追问：缓存和数据库不一致怎么办？
   - 追问：MQ 重复消费导致订单重复怎么办？

9. 项目里有哪些并发安全问题？
   - 追问：超卖、重复下单、重复消费、支付关单并发分别如何处理？
   - 追问：哪些地方依赖 Redis 原子性，哪些地方依赖数据库条件更新？

10. 如果面试官问“这个项目有什么亮点”，你会讲哪 3 个？
    - 追问：每个亮点背后的技术难点是什么？
    - 追问：这些亮点有没有量化结果，比如 TPS、延迟、错误率？

11. 如果让你重构这个项目，你觉得当前设计哪里还可以优化？
    - 追问：哪些地方是 demo 项目可以接受，但生产环境需要补齐的？
    - 追问：你会如何补充监控、告警、降级和补偿机制？

12. 这个项目有没有做分层？Controller、Service、MQ Consumer、Canal Handler 的职责分别是什么？
    - 追问：为什么不要把业务逻辑写在 Controller 里？
    - 追问：MQ Consumer 里为什么要尽量保持薄逻辑？

### 2. Redis 库存预扣减、Lua 脚本与一人一单

1. 为什么秒杀时先扣 Redis 库存，而不是直接扣 MySQL 库存？
   - 追问：Redis 预扣减会带来哪些一致性问题？
   - 追问：如果 Redis 扣成功但 MQ 发送失败怎么办？

2. Lua 脚本在秒杀链路中做了哪些事情？
   - 追问：为什么库存判断、扣减、一人一单检查要放在同一个 Lua 脚本里？
   - 追问：Lua 脚本的原子性是怎么保证的？

3. 你的 Lua 脚本返回值 0、1、2 分别代表什么？
   - 追问：业务层如何根据不同返回值给用户响应？
   - 追问：如果脚本执行异常，应该返回什么？

4. 一人一单为什么用 Redis Set 存用户 id？
   - 追问：Set 的内存占用会不会很高？
   - 追问：如果秒杀活动非常多，订单 Set 如何清理？

5. 只依赖 Redis Set 做一人一单够不够？
   - 追问：为什么消费者落库时还要再查数据库？
   - 追问：数据库层是否需要唯一索引兜底？

6. Redis 里库存扣减成功，但最终数据库库存扣减失败，怎么补偿？
   - 追问：补偿操作需要具备幂等性吗？为什么？
   - 追问：补偿失败后如何发现和修复？

7. Redis 库存初始化从哪里来？什么时候写入 Redis？
   - 追问：如果后台修改了秒杀库存，Redis 中的库存如何同步？
   - 追问：库存初始化时是否要考虑并发写入？

8. 如果 Redis 宕机，秒杀接口怎么办？
   - 追问：是直接降级、限流，还是切回数据库？
   - 追问：如何避免 Redis 恢复后库存数据错乱？

9. Lua 脚本执行时间过长会有什么风险？
   - 追问：Redis 单线程执行模型下，慢脚本会影响什么？
   - 追问：如何排查 Redis slowlog？

10. Redis Cluster 环境下 Lua 脚本有什么限制？
    - 追问：KEYS 必须落在同一个 hash slot 怎么处理？
    - 追问：你当前脚本把 key 作为 ARGV 传入有什么隐患？

11. 秒杀库存 key 和订单 Set key 如何设计？
    - 追问：key 命名如何避免冲突？
    - 追问：优惠券秒杀和商品秒杀为什么要区分 key 前缀？

12. 如何防止超卖？
    - 追问：Redis 层怎么防？MySQL 层怎么兜底？
    - 追问：如果两个消费者同时扣数据库库存，会不会超卖？

13. 如果用户短时间内重复点击秒杀按钮，会发生什么？
    - 追问：Redis 层如何拦截？
    - 追问：接口限流是否还需要做？

14. Lua 脚本里为什么要先判断库存再判断用户是否下单？
    - 追问：先判断用户再判断库存有没有区别？
    - 追问：不同顺序对错误提示、性能和攻击防护有什么影响？

15. Redis 预扣成功就返回 orderId，这个 orderId 是怎么生成的？
    - 追问：如果订单最终落库失败，用户拿到的 orderId 如何处理？
    - 追问：订单状态是否需要一个“创建中”状态？

### 3. RocketMQ 异步下单与削峰

1. 为什么引入 RocketMQ 做异步下单？
   - 追问：它解决了 MySQL 的什么压力？
   - 追问：异步化后接口响应时间为什么会降低？

2. 秒杀请求进入 MQ 前后，系统瓶颈发生了什么变化？
   - 追问：从“数据库写入瓶颈”变成了什么瓶颈？
   - 追问：Consumer 消费能力不足时会发生什么？

3. 你的下单消息里包含哪些字段？
   - 追问：为什么消息里要带 orderId、userId、商品/券 id？
   - 追问：消息体太大或太小分别有什么问题？

4. 发送 MQ 消息失败后你怎么处理？
   - 追问：为什么要回滚 Redis 库存和用户下单 Set？
   - 追问：这个回滚操作如果重复执行会有什么问题？

5. RocketMQ 消费失败会怎么重试？
   - 追问：消费者抛异常和吞异常有什么区别？
   - 追问：重试多次后进入死信队列怎么办？

6. 如何保证消费者幂等？
   - 追问：为什么先根据 orderId 查订单？
   - 追问：为什么还要根据 userId + voucherId/productId 查已有订单？

7. MQ 重复消费时会不会重复扣数据库库存？
   - 追问：当前代码如何避免？
   - 追问：数据库唯一索引和业务查询哪个更可靠？

8. 你的消费者里为什么还用了 Redisson 锁？
   - 追问：锁的粒度为什么按 userId 或 productId:userId？
   - 追问：如果拿不到锁，抛异常重试是否合适？

9. RocketMQ 如何保证消息至少被消费一次？
   - 追问：至少一次语义为什么会导致重复消费？
   - 追问：业务上如何接受“至少一次”而不是“刚好一次”？

10. 如果 MQ 积压严重，你怎么排查？
    - 追问：看哪些指标？生产速率、消费速率、堆积量、消费耗时？
    - 追问：扩容 Consumer 是否一定有效？

11. 如果消费者落库成功，但发送延迟关单消息失败怎么办？
    - 追问：订单会不会永远未支付？
    - 追问：是否需要定时扫描未支付订单兜底？

12. 为什么订单创建需要放在事务里？
    - 追问：扣数据库库存和保存订单哪个应该先做？
    - 追问：事务只能保证数据库，不能保证 Redis/MQ，怎么理解？

13. RocketMQ Topic 和 ConsumerGroup 怎么设计？
    - 追问：秒杀下单、商品下单、超时关单、Canal 缓存同步为什么要拆 Topic？
    - 追问：同一个 ConsumerGroup 下多个实例消费模式是什么？

14. 如果要保证同一个用户的订单消息顺序消费，你会怎么做？
    - 追问：顺序消息的 MessageQueue 选择依据是什么？
    - 追问：这个项目是否真的需要顺序消息？

15. RocketMQ 和 Kafka、RabbitMQ 相比，为什么这里选择 RocketMQ？
    - 追问：RocketMQ 在延迟消息、事务消息、顺序消息方面有什么特点？
    - 追问：如果面试官问你是否用过 RocketMQ 事务消息，你怎么回答？

### 4. 延迟消息、超时关单与支付并发

1. 为什么需要超时关单？
   - 追问：如果用户下单后一直不支付，会影响什么？
   - 追问：库存占用和真实库存扣减的关系是什么？

2. RocketMQ 延迟消息如何实现订单 30 分钟后关闭？
   - 追问：RocketMQ 4.x 延迟消息为什么是固定等级？
   - 追问：如果要支持任意延迟时间怎么设计？

3. 延迟关单消息发送的时机是什么？
   - 追问：为什么要在订单成功创建后发送？
   - 追问：如果重复消费下单消息，会不会重复发送延迟关单消息？

4. 关单消费者收到消息后做了什么？
   - 追问：为什么要先查订单状态？
   - 追问：订单不存在、已支付、已取消分别怎么处理？

5. 关单和支付回调并发时，如何保证订单状态正确？
   - 追问：为什么更新订单时要带 status = UNPAID 条件？
   - 追问：商品订单里 version 乐观锁起什么作用？

6. 什么是乐观锁？为什么这里适合乐观锁？
   - 追问：乐观锁失败说明什么？
   - 追问：乐观锁和悲观锁在高并发下的取舍是什么？

7. 超时关单成功后，为什么要回滚库存？
   - 追问：需要回滚 MySQL 库存和 Redis 库存吗？
   - 追问：只回滚数据库、不回滚 Redis 会有什么后果？

8. Redis 回滚为什么要做幂等控制？
   - 追问：为什么使用 order close key 做 setnx？
   - 追问：如果重复关单消息来了，会不会重复加库存？

9. 如果支付成功后又收到了延迟关单消息，会发生什么？
   - 追问：当前状态判断能不能拦住？
   - 追问：如果支付回调和关单几乎同时 update，谁赢？

10. 如果延迟关单消息丢失怎么办？
    - 追问：RocketMQ 是否保证消息绝对不丢？
    - 追问：是否需要定时任务扫描超时未支付订单？

11. 如果关单成功但 Redis 库存回滚失败怎么办？
    - 追问：数据库事务已经提交后，Redis 失败如何补偿？
    - 追问：是否需要补偿消息或重试任务？

12. 订单状态有哪些？状态流转图是什么？
    - 追问：哪些状态可以被支付？哪些状态可以被关闭？
    - 追问：是否允许取消后重新购买？

13. 支付接口为什么要校验订单所属用户？
    - 追问：如果不校验，会有什么安全问题？
    - 追问：真实支付回调通常没有用户登录态，应该怎么验签？

14. 为什么商品订单设置了 version，而优惠券订单主要使用 status 条件？
    - 追问：两种方式有什么区别？
    - 追问：只用 status 条件算不算乐观锁？

15. 订单超时时间配置在哪里更合适？
    - 追问：写死在常量里有什么问题？
    - 追问：如果不同商品超时时间不同，怎么扩展？

### 5. 热点数据缓存、Canal 与最终一致性

1. 为什么店铺或热点商品查询适合使用 Redis 缓存？
   - 追问：哪些数据适合缓存，哪些不适合缓存？
   - 追问：缓存命中率怎么衡量？

2. 查询店铺时的缓存流程是什么？
   - 追问：命中缓存、命中空值、缓存未命中分别怎么走？
   - 追问：缓存对象如何序列化和反序列化？

3. 更新店铺信息时为什么要删除缓存，而不是更新缓存？
   - 追问：先更新数据库再删缓存，还是先删缓存再更新数据库？
   - 追问：删除缓存失败怎么办？

4. 你为什么引入 Canal？
   - 追问：Canal 基于什么原理感知数据库变更？
   - 追问：binlog row 模式和 statement 模式有什么区别？

5. Canal + RocketMQ 在项目中怎么协作？
   - 追问：Canal 监听 MySQL binlog 后如何投递到 MQ？
   - 追问：应用侧如何消费 Canal flat message？

6. Canal 消费者如何根据表名分发到不同缓存处理器？
   - 追问：为什么要设计 CanalCacheHandler 接口？
   - 追问：后续新增表的缓存同步怎么扩展？

7. Canal 监听到 tb_shop 更新后做了什么？
   - 追问：为什么删除 cache:shop:id？
   - 追问：为什么还要同步 GEO 缓存？

8. Canal 消息重复消费会不会有问题？
   - 追问：删除缓存天然幂等吗？
   - 追问：GEO add/remove 重复执行是否安全？

9. Canal 消息延迟会导致什么一致性问题？
   - 追问：这段时间用户可能读到旧缓存，是否可接受？
   - 追问：什么叫最终一致性？

10. 如果数据库更新成功，但应用主动删除缓存失败，Canal 能否兜底？
    - 追问：Canal 也失败怎么办？
    - 追问：是否需要缓存定期过期作为最终兜底？

11. 如果 Canal 消息先后乱序，会带来什么问题？
    - 追问：删除缓存方案为什么比更新缓存更能容忍乱序？
    - 追问：如果是直接写缓存，如何处理版本号？

12. Redis 缓存如何设置 TTL？
    - 追问：热点缓存是否一定要 TTL？
    - 追问：TTL 太短或太长分别有什么问题？

13. 缓存和数据库双写一致性有哪些常见方案？
    - 追问：Cache Aside、延迟双删、订阅 binlog 的优缺点是什么？
    - 追问：你的项目属于哪种方案？

14. 如何证明缓存带来了 30% 查询延迟下降？
    - 追问：测试条件是什么？命中率是多少？
    - 追问：平均延迟、P95、P99 哪个更能说明问题？

15. 如果 Redis 里缓存的是脏数据，如何快速修复？
    - 追问：按 key 删除、批量失效、版本号隔离怎么选？
    - 追问：线上是否允许直接 flushdb？

### 6. 缓存穿透、击穿、雪崩

1. 什么是缓存穿透？你的项目怎么解决？
   - 追问：缓存空值有什么副作用？
   - 追问：恶意请求大量随机 id 时，空值缓存够不够？

2. 什么是缓存击穿？热点 key 过期时会发生什么？
   - 追问：为什么大量请求会同时打到数据库？
   - 追问：互斥锁和逻辑过期分别怎么解决？

3. 什么是缓存雪崩？你的项目用了什么策略？
   - 追问：随机 TTL 为什么能缓解雪崩？
   - 追问：Redis 集群宕机导致的雪崩怎么处理？

4. 缓存空值的 TTL 为什么要比正常数据短？
   - 追问：如果数据库后来新增了这个 id，空值缓存会不会影响查询？
   - 追问：空值 TTL 太长有什么问题？

5. 互斥锁缓存重建流程是什么？
   - 追问：没抢到锁的线程是等待、重试还是直接返回？
   - 追问：如何避免死锁？

6. 逻辑过期方案的流程是什么？
   - 追问：为什么逻辑过期可以提高可用性？
   - 追问：它会不会返回旧数据？

7. 互斥锁和逻辑过期如何选择？
   - 追问：强一致场景选哪个？
   - 追问：热点数据高可用场景选哪个？

8. 你的 CacheClient 做了哪些通用封装？
   - 追问：为什么要用泛型和 dbFallback 函数？
   - 追问：这种封装有什么可复用价值？

9. Redis 锁为什么要设置过期时间？
   - 追问：业务执行时间超过锁 TTL 怎么办？
   - 追问：为什么 Redisson 的看门狗机制能缓解这个问题？

10. 删除锁时如何避免误删别人的锁？
    - 追问：简单 delete 有什么风险？
    - 追问：为什么通常要 value 唯一标识 + Lua 删除？

11. 缓存重建失败怎么办？
    - 追问：是否要释放锁？
    - 追问：是否要记录日志、告警、降级？

12. 如果热点 key 很多，逻辑过期线程池不够用怎么办？
    - 追问：线程池参数如何设计？
    - 追问：重建任务是否需要合并？

13. 如何预热热点缓存？
    - 追问：项目启动预热、定时预热、活动开始前预热怎么选？
    - 追问：预热失败对秒杀链路有什么影响？

14. 布隆过滤器能解决什么问题？
    - 追问：相比缓存空值有什么优缺点？
    - 追问：布隆过滤器误判会带来什么影响？

15. 缓存策略如何和 Canal 最终一致性配合？
    - 追问：Canal 删除缓存后，下次查询如何重建？
    - 追问：逻辑过期缓存被 Canal 删除后是否有问题？

### 7. Redis + AOP + 注解限流

1. 为什么要做接口限流？
   - 追问：限流和熔断、降级有什么区别？
   - 追问：秒杀接口为什么尤其需要限流？

2. 你的限流注解支持哪些配置？
   - 追问：name、key、scopes、limit、window 分别有什么作用？
   - 追问：为什么要支持重复注解 RateLimits？

3. AOP 是如何拦截带有 @RateLimit 的方法的？
   - 追问：Around 通知为什么适合做限流？
   - 追问：限流切面执行顺序为什么要设置较高优先级？

4. 限流支持哪些维度？
   - 追问：GLOBAL、IP、USER 分别适合什么场景？
   - 追问：匿名用户如何处理 USER 维度？

5. 限流 key 是怎么生成的？
   - 追问：为什么要包含算法、接口名、业务 key、scope？
   - 追问：如果 key 设计过粗或过细会怎样？

6. 业务 key 为什么支持 SpEL？
   - 追问：#productId 是如何解析出来的？
   - 追问：SpEL 解析失败应该怎么处理？

7. 滑动窗口限流的原理是什么？
   - 追问：为什么用 ZSet 存请求时间戳？
   - 追问：每次请求为什么要 zremrangebyscore 清理窗口外数据？

8. 滑动窗口相比固定窗口有什么优势？
   - 追问：固定窗口临界点突刺是什么？
   - 追问：滑动窗口的内存成本是什么？

9. 令牌桶限流的原理是什么？
   - 追问：bucketCapacity、refillTokens、refillInterval、permits 分别代表什么？
   - 追问：令牌桶为什么允许一定突发流量？

10. 为什么限流逻辑也用 Lua 脚本？
    - 追问：ZSet 清理、计数、写入为什么要原子？
    - 追问：Hash 中 token refill 和扣减为什么要原子？

11. 如何获取真实客户端 IP？
    - 追问：X-Forwarded-For 可以被伪造吗？
    - 追问：在 Nginx 网关后面如何可信地取 IP？

12. 限流失败后如何返回给前端？
    - 追问：为什么要抛 RateLimitException？
    - 追问：全局异常处理应该返回什么 HTTP 状态码？

13. 全局限流、IP 限流、用户限流同时配置时，执行顺序重要吗？
    - 追问：哪个维度应该最先拦截？
    - 追问：任一维度失败就拒绝是否合理？

14. Redis 限流 key 的 TTL 如何设置？
    - 追问：滑动窗口 key 什么时候过期？
    - 追问：令牌桶 key 过期时间如何计算？

15. 如果 Redis 不可用，限流应该失败开放还是失败关闭？
    - 追问：业务安全和系统可用性如何权衡？
    - 追问：秒杀接口和普通查询接口选择是否一样？

### 8. Redis ZSet 点赞排行榜

1. 为什么笔记点赞列表使用 Redis ZSet？
   - 追问：ZSet 的 member 和 score 分别存什么？
   - 追问：为什么不用 List 或 Set？

2. 点赞和取消点赞的流程是什么？
   - 追问：如何判断用户是否已经点赞？
   - 追问：为什么要同时更新数据库 liked 字段和 Redis ZSet？

3. 查询前 5 个点赞用户时为什么按 score 升序？
   - 追问：score 使用时间戳意味着排序含义是什么？
   - 追问：如果要查最新点赞用户，应该用哪个命令？

4. ZSet 如何保证用户不会重复点赞？
   - 追问：同一个 member 重复 zadd 会发生什么？
   - 追问：数据库 liked 计数会不会重复加？

5. Redis 点赞状态和数据库 liked 计数不一致怎么办？
   - 追问：先更新数据库还是先更新 Redis？
   - 追问：失败补偿如何设计？

6. 当前点赞逻辑并发下会有什么问题？
   - 追问：两个请求同时点赞同一篇笔记，liked 会不会多加？
   - 追问：是否需要 Lua 或数据库唯一约束兜底？

7. 为什么查询点赞用户后还要按 FIELD(id, ...) 保持顺序？
   - 追问：MySQL 的 in 查询默认会保持输入顺序吗？
   - 追问：如果不用 ORDER BY FIELD 会怎样？

8. 热门笔记 queryHotBlog 为什么按 liked 排序？
   - 追问：liked 字段是否可能和 Redis ZSet 不一致？
   - 追问：热门榜是否应该完全放到 Redis 里？

9. 如果一篇爆款笔记有百万点赞，ZSet 会有什么压力？
   - 追问：内存如何估算？
   - 追问：是否需要只保留前 N 个用户？

10. 点赞数据是否需要持久化明细表？
    - 追问：只存 ZSet 有什么风险？
    - 追问：如果要做用户点赞历史，怎么建表？

11. Redis 持久化丢失点赞数据怎么办？
    - 追问：RDB/AOF 对数据恢复有什么影响？
    - 追问：是否需要以 MySQL 为准定期校准？

12. 如何设计“共同关注”和“关注 Feed 流”？
    - 追问：Feed 使用推模式还是拉模式？
    - 追问：项目里的收件箱 ZSet 如何支持滚动分页？

13. ZSet score 用 System.currentTimeMillis 有什么问题？
    - 追问：多机器时钟不一致怎么办？
    - 追问：同一毫秒多个用户点赞如何排序？

14. 点赞接口是否需要限流？
    - 追问：恶意刷赞怎么防？
    - 追问：是否需要风控、验证码或行为分析？

15. 如何从产品角度解释点赞榜的实时性？
    - 追问：是强实时还是准实时？
    - 追问：延迟几秒是否可接受？

### 9. Redis 全局 ID 生成器

1. 为什么订单 id 不直接使用数据库自增 id？
   - 追问：高并发下数据库自增 id 有什么问题？
   - 追问：分库分表后自增 id 会遇到什么问题？

2. RedisIdWorker 的 ID 结构是什么？
   - 追问：时间戳部分和序列号部分分别占多少位？
   - 追问：为什么要设置 BEGIN_TIMESTAMP？

3. 为什么每天一个 Redis 自增 key？
   - 追问：这样做对序列号位数有什么好处？
   - 追问：这些自增 key 是否需要设置过期时间？

4. 这个 ID 生成方案是否全局唯一？
   - 追问：多实例部署时是否会冲突？
   - 追问：Redis 主从切换时有没有风险？

5. 这个 ID 是否趋势递增？
   - 追问：趋势递增对数据库索引有什么好处？
   - 追问：和 UUID 相比有什么优势？

6. 如果系统时间回拨，会影响 ID 生成吗？
   - 追问：如何检测和处理时钟回拨？
   - 追问：为什么雪花算法也需要考虑这个问题？

7. Redis 自增达到 32 位上限怎么办？
   - 追问：一天内能支持多少个 ID？
   - 追问：如果业务量超过上限，如何扩展？

8. 为什么 timestamp 使用 UTC 秒？
   - 追问：本地时区变化会不会影响？
   - 追问：订单号是否需要具备业务可读性？

9. ID 生成失败时下单链路怎么处理？
   - 追问：Redis 短暂不可用是否可以本地降级生成？
   - 追问：降级生成如何避免冲突？

10. 和 Snowflake、美团 Leaf、数据库号段相比，你这个方案优缺点是什么？
    - 追问：生产环境你更倾向用哪种？
    - 追问：号段模式如何减少 Redis/DB 调用？

### 10. Redis GEO 附近店铺

1. 项目中附近店铺功能如何实现？
   - 追问：Redis GEO 底层基于什么数据结构？
   - 追问：经纬度数据存在哪里？

2. 查询附近店铺时为什么先查 Redis GEO，再查 MySQL？
   - 追问：Redis 返回的是哪些信息？
   - 追问：为什么还需要回表查店铺详情？

3. GEO 查询如何分页？
   - 追问：为什么先 limit 到 end，再 skip from？
   - 追问：这种分页方式有什么性能问题？

4. 查询出来的店铺如何保持距离排序？
   - 追问：为什么要使用 ORDER BY FIELD？
   - 追问：MySQL 查询结果默认顺序可靠吗？

5. Canal 更新店铺时为什么要同步 GEO 缓存？
   - 追问：店铺经纬度变更如何处理？
   - 追问：店铺类型 type_id 变更如何处理？

6. Redis GEO 的距离计算精度如何？
   - 追问：适不适合严肃地图导航？
   - 追问：本地生活场景是否够用？

7. 如果附近店铺数据量很大，如何优化？
   - 追问：按城市、类型拆 key 是否有必要？
   - 追问：半径过大有什么问题？

8. 如果用户不传经纬度，接口如何降级？
   - 追问：为什么可以退化成按类型分页？
   - 追问：产品上是否需要提示用户授权定位？

9. 店铺新增、删除、修改时 GEO 缓存如何保持一致？
   - 追问：主动更新和 Canal 更新哪个更可靠？
   - 追问：重复 remove/add 是否有问题？

10. Redis GEO key 如何设计？
    - 追问：为什么按 typeId 分组？
    - 追问：如果加入城市维度，key 应该怎么改？

## 二、按面试官视角拆解

### 1. 项目真实性与个人贡献视角

1. 这个项目是课程项目、个人项目还是实习项目？你做了哪些二次改造？
   - 追问：哪些代码是你新增的？能指出具体类吗？

2. 你为什么把项目叫“趣享生活”？和黑马点评原项目有什么区别？
   - 追问：你新增了商品秒杀、RocketMQ、Canal、限流后，原链路发生了什么变化？

3. 你在项目中遇到的最大技术难点是什么？
   - 追问：你当时有哪些备选方案？
   - 追问：最后为什么选择现在这个方案？

4. 你怎么证明本机压测 TPS 达到 2000？
   - 追问：用什么压测工具？并发数、请求数、机器配置是什么？
   - 追问：接口平均耗时、P95、错误率是多少？

5. 项目上线了吗？如果没有，怎么模拟线上环境？
   - 追问：Docker Compose 起了哪些组件？
   - 追问：本地环境和生产环境差异在哪里？

6. 你有没有写测试？覆盖了哪些场景？
   - 追问：秒杀并发测试怎么写？
   - 追问：MQ 消费幂等怎么测试？

7. 你如何向非技术面试官解释这个项目价值？
   - 追问：对用户、商家、平台分别有什么价值？
   - 追问：高并发秒杀为什么是本地生活平台的典型场景？

8. 如果面试官打开你的代码，你最想让他看哪几个类？
   - 追问：VoucherOrderServiceImpl、ProductOrderServiceImpl、RateLimitAspect、CacheClient、CanalFlatMessageConsumer 分别体现什么能力？

9. 这个项目中你最满意的一处设计是什么？
   - 追问：它相比简单实现好在哪里？
   - 追问：有没有进一步优化空间？

10. 这个项目中你最不满意的一处是什么？
    - 追问：如果有时间，你会优先补什么？
    - 追问：为什么现在没有补？

11. 简历上写“延迟降低 30%”，这个数字怎么来的？
    - 追问：缓存命中和未命中的对比怎么做？
    - 追问：是否排除了网络、JVM 预热、连接池等因素？

12. 如果我让你现场画架构图，你会怎么画？
    - 追问：请求链路、MQ 链路、Canal 链路、Redis 缓存链路分别怎么画？

### 2. 系统设计与架构取舍视角

1. 请你设计一个本地生活平台的高并发秒杀系统。
   - 追问：前端、网关、应用、缓存、MQ、数据库分别做什么？

2. 秒杀系统为什么要做削峰？
   - 追问：削峰和限流有什么区别？
   - 追问：MQ 削峰会不会牺牲实时性？

3. 如何设计库存系统？
   - 追问：可售库存、锁定库存、已售库存如何区分？
   - 追问：你当前项目属于哪种库存模型？

4. 如何设计订单状态机？
   - 追问：未支付、已支付、已取消、超时关闭之间如何流转？
   - 追问：状态流转如何防止越权和重复？

5. 如果用户支付成功，但库存回滚已经发生，怎么办？
   - 追问：如何避免这种状态？
   - 追问：如果真的发生，补偿策略是什么？

6. 如何保证系统高可用？
   - 追问：Redis、MQ、MySQL 任一组件不可用时如何降级？
   - 追问：哪些接口可以失败开放，哪些必须失败关闭？

7. 如何设计接口限流体系？
   - 追问：全局限流、用户限流、IP 限流分别放在哪一层？
   - 追问：应用层限流和网关层限流如何配合？

8. 如何设计缓存体系？
   - 追问：哪些缓存本地化，哪些放 Redis？
   - 追问：热点 key 如何发现和保护？

9. 如果要支持多城市、多商户、多活动，你会如何扩展模型？
   - 追问：Redis key、数据库索引、MQ Topic 是否要拆分？
   - 追问：是否需要分库分表？

10. 这个系统如何做灰度发布？
    - 追问：新旧版本消费者同时存在会不会重复消费？
    - 追问：消息格式变更如何兼容？

11. 如何设计后台管理对秒杀活动的发布、暂停、恢复？
    - 追问：活动状态如何同步到 Redis？
    - 追问：活动下线后 Redis key 如何处理？

12. 如果业务要求库存绝对不能错，你会怎么加强当前方案？
    - 追问：数据库约束、对账任务、补偿任务、审计日志如何设计？

13. 如何处理热点商品导致单 key 过热？
    - 追问：库存分片怎么做？
    - 追问：分片库存如何汇总和回滚？

14. 如何设计风控防刷？
    - 追问：除了限流，还有哪些策略？
    - 追问：设备指纹、验证码、黑名单、行为分如何接入？

15. 如果从单体改成微服务，你会怎么拆？
    - 追问：用户、店铺、商品、订单、内容、支付服务如何划分？
    - 追问：分布式事务怎么处理？

### 3. Redis 深度视角

1. Redis 为什么快？
   - 追问：单线程模型、IO 多路复用、内存访问分别起什么作用？

2. Redis 单线程为什么还能支撑高并发？
   - 追问：Redis 6 多线程主要多线程在哪里？
   - 追问：命令执行是否多线程？

3. 项目中 Redis 使用了哪些数据结构？
   - 追问：每种结构对应哪个业务？
   - 追问：为什么不用一种结构解决所有问题？

4. Redis key 过期策略是什么？
   - 追问：定期删除、惰性删除怎么工作？
   - 追问：过期 key 太多会影响什么？

5. Redis 内存淘汰策略有哪些？
   - 追问：volatile-lru、allkeys-lru、noeviction 有什么区别？
   - 追问：你的缓存场景适合哪种？

6. Redis 持久化 RDB 和 AOF 有什么区别？
   - 追问：秒杀库存数据适合依赖 Redis 持久化恢复吗？
   - 追问：AOF everysec 可能丢多少数据？

7. Redis 主从复制和哨兵是什么？
   - 追问：主从切换时可能丢数据吗？
   - 追问：秒杀扣库存时如何应对主从延迟？

8. Redis Cluster 如何分片？
   - 追问：hash slot 是什么？
   - 追问：多 key Lua 在 Cluster 下有什么限制？

9. Redis 分布式锁有哪些坑？
   - 追问：锁过期、误删锁、不可重入、主从切换分别有什么问题？
   - 追问：Redisson 如何解决一部分问题？

10. Redis 事务和 Lua 脚本有什么区别？
    - 追问：MULTI/EXEC 能否保证复杂逻辑原子？
    - 追问：Lua 脚本能否回滚？

11. Redis 热 key 怎么发现？
    - 追问：monitor、hotkeys、客户端统计、代理层统计怎么用？
    - 追问：热 key 如何拆分？

12. Redis 大 key 有什么危害？
    - 追问：Set/ZSet 存大量用户 id 会不会成为大 key？
    - 追问：怎么拆分或清理？

13. Redis 连接池参数如何设置？
    - 追问：max-active、max-idle、min-idle 影响什么？
    - 追问：连接池耗尽时表现是什么？

14. Redis 和本地缓存相比怎么选？
    - 追问：本地缓存一致性如何处理？
    - 追问：热点数据是否可以做二级缓存？

15. Redis 限流和网关限流哪个更好？
    - 追问：分布式一致性、性能、维护成本如何权衡？

### 4. RocketMQ 深度视角

1. RocketMQ 的核心组件有哪些？
   - 追问：NameServer、Broker、Producer、Consumer 分别负责什么？

2. RocketMQ 消息发送流程是什么？
   - 追问：Producer 如何找到 Broker？
   - 追问：同步发送、异步发送、单向发送有什么区别？

3. RocketMQ 消费模式有哪些？
   - 追问：集群消费和广播消费有什么区别？
   - 追问：你的项目使用哪种？

4. RocketMQ 如何处理消费失败？
   - 追问：重试队列、死信队列是什么？
   - 追问：业务上如何处理死信消息？

5. 为什么 MQ 不能保证业务天然幂等？
   - 追问：重复投递、消费超时、消费者重启分别如何导致重复？
   - 追问：幂等应该由 MQ 保证还是业务保证？

6. RocketMQ 延迟消息的限制是什么？
   - 追问：固定延迟等级有什么不方便？
   - 追问：RocketMQ 5.x 定时消息有什么变化？

7. RocketMQ 事务消息能解决什么问题？
   - 追问：能不能用事务消息解决 Redis 扣库存和 MQ 发送一致性？
   - 追问：事务消息的半消息和回查机制是什么？

8. 如何避免消息丢失？
   - 追问：Producer、Broker、Consumer 三端分别怎么保证？
   - 追问：同步刷盘和异步刷盘如何取舍？

9. 如何避免消息积压？
   - 追问：生产限速、消费者扩容、批量消费、优化消费逻辑分别怎么做？
   - 追问：数据库写入慢导致积压怎么办？

10. 顺序消息适合什么场景？
    - 追问：订单状态流转是否需要顺序消息？
    - 追问：顺序消息会降低并发吗？

11. Topic、Tag、ConsumerGroup 如何规划？
    - 追问：什么时候拆 Topic，什么时候用 Tag？
    - 追问：一个 ConsumerGroup 下不同代码版本会有什么问题？

12. 消息体设计要注意什么？
    - 追问：消息里是否应该只放 id？
    - 追问：消息 schema 演进如何兼容？

13. MQ 和线程池异步有什么区别？
    - 追问：可靠性、削峰能力、跨进程能力有什么不同？
    - 追问：什么时候用线程池就够了？

14. 如果 RocketMQ 挂了，秒杀还能继续吗？
    - 追问：直接失败、降级排队、本地队列暂存各有什么风险？
    - 追问：本地队列宕机丢失怎么办？

15. 如何监控 RocketMQ？
    - 追问：消息堆积、消费失败、重试次数、Broker 磁盘水位哪些要告警？

### 5. MySQL 与事务视角

1. 秒杀数据库扣库存为什么要加 stock > 0 条件？
   - 追问：它如何防止超卖？
   - 追问：高并发下 update set stock = stock - 1 where stock > 0 是否安全？

2. 一人一单在数据库层如何兜底？
   - 追问：是否应该建 user_id + voucher_id/product_id 唯一索引？
   - 追问：取消订单后是否允许再次下单，唯一索引怎么设计？

3. MySQL 事务隔离级别有哪些？
   - 追问：默认 RR 能解决哪些问题？
   - 追问：当前项目主要依赖隔离级别还是条件更新？

4. 什么是幻读？这个项目会遇到吗？
   - 追问：查询已有订单再插入订单是否有幻读风险？
   - 追问：唯一索引如何兜底？

5. MySQL 行锁什么时候生效？
   - 追问：没有走索引会发生什么？
   - 追问：秒杀扣库存 update 会锁哪一行？

6. 乐观锁和数据库行锁是什么关系？
   - 追问：version 字段如何防止并发覆盖？
   - 追问：status 条件更新算不算 CAS？

7. 为什么保存订单和扣数据库库存要在同一个事务？
   - 追问：库存扣成功、订单保存失败怎么办？
   - 追问：事务回滚后 Redis 已扣库存怎么办？

8. 如何设计订单表索引？
   - 追问：按 user_id、product_id/voucher_id 查询要建什么索引？
   - 追问：超时未支付扫描要建什么索引？

9. 如果订单表数据量非常大，如何分表？
   - 追问：按用户、按订单 id、按时间分表怎么选？
   - 追问：分表后如何查询用户订单列表？

10. MyBatis-Plus update 链式调用最终生成什么 SQL？
    - 追问：setSql 有 SQL 注入风险吗？
    - 追问：last("LIMIT 1") 使用时要注意什么？

11. 什么情况下事务会失效？
    - 追问：同类内部方法调用为什么可能绕过代理？
    - 追问：你项目里为什么使用 TransactionTemplate？

12. 数据库和缓存一致性为什么不能靠一个本地事务解决？
    - 追问：事务只能覆盖哪些资源？
    - 追问：跨 Redis、MQ、MySQL 如何做最终一致？

13. Flyway 在项目中起什么作用？
    - 追问：数据库迁移脚本为什么要版本化？
    - 追问：多人协作时 migration 冲突怎么处理？

14. 慢 SQL 如何排查？
    - 追问：Explain 重点看哪些字段？
    - 追问：filesort、temporary、全表扫描如何优化？

15. 如果 MySQL 主从延迟，读写分离会影响哪些功能？
    - 追问：下单后立即查订单可能读不到怎么办？
    - 追问：缓存重建从从库读到旧数据怎么办？

### 6. 数据一致性与可靠性视角

1. 这个项目有哪些最终一致性场景？
   - 追问：Redis 库存与 MySQL 库存、缓存与数据库、MQ 消息与订单状态分别怎么一致？

2. 什么是幂等？项目里哪些操作必须幂等？
   - 追问：下单消费、关单消费、Redis 回滚、Canal 删除缓存分别如何幂等？

3. 什么是补偿？项目里有哪些补偿动作？
   - 追问：MQ 发送失败回滚 Redis 是补偿吗？
   - 追问：关单回滚库存失败如何补偿？

4. Redis 预扣和 MQ 发送之间不是事务，怎么保证可靠？
   - 追问：当前方案有哪些漏洞？
   - 追问：是否可以用本地消息表、事务消息或重试队列增强？

5. 订单创建成功但响应给用户失败怎么办？
   - 追问：用户重试会不会重复下单？
   - 追问：前端如何根据 orderId 或业务状态查询结果？

6. 消费者扣数据库库存失败后如何处理 Redis 预扣库存？
   - 追问：返回 null 不抛异常和抛异常重试有什么区别？
   - 追问：哪些失败适合重试，哪些失败适合直接补偿？

7. 如何做对账？
   - 追问：Redis 秒杀库存、MySQL 秒杀库存、订单数量如何对账？
   - 追问：对账发现不一致如何修复？

8. 如果重复关单消息导致多次库存回滚，怎么避免？
   - 追问：数据库状态判断够不够？
   - 追问：Redis setnx close key 起什么作用？

9. Canal 消费失败会怎样？
   - 追问：抛异常让 MQ 重试是否合理？
   - 追问：重试仍失败是否进死信，如何恢复？

10. 删除缓存失败后如何保证最终删除？
    - 追问：重试、延迟双删、binlog 监听、TTL 兜底怎么组合？

11. 如何设计可靠的支付回调处理？
    - 追问：第三方支付可能重复回调吗？
    - 追问：回调验签、幂等、订单状态 CAS 如何做？

12. 如果应用宕机发生在不同步骤，会有什么结果？
    - 追问：Redis 扣完库存宕机、MQ 发完宕机、订单保存完宕机分别怎么恢复？

13. 如何保证用户看到的订单状态可信？
    - 追问：是否需要订单查询接口轮询？
    - 追问：异步下单中的“处理中”状态如何设计？

14. 最终一致性和强一致性的边界怎么划？
    - 追问：哪些场景必须强一致？
    - 追问：哪些场景可以接受短暂不一致？

15. 如果老板要求“绝不丢单”，你会加哪些机制？
    - 追问：请求流水、本地消息表、订单创建状态机、补偿任务、告警如何设计？

### 7. 高并发、性能压测与优化视角

1. 你是怎么压测秒杀接口的？
   - 追问：使用 JMeter、wrk、ab 还是其他工具？
   - 追问：线程数、连接数、持续时间怎么设置？

2. TPS 达 2000 的测试条件是什么？
   - 追问：本机 CPU、内存、Redis/MySQL/MQ 是否都在本机？
   - 追问：是否包含登录鉴权、网络开销和数据库落库？

3. 压测时关注哪些指标？
   - 追问：TPS、QPS、平均响应时间、P95、P99、错误率分别说明什么？

4. 如何定位性能瓶颈？
   - 追问：应用 CPU 高、Redis 慢、MySQL 慢、MQ 积压分别怎么判断？

5. 秒杀接口为什么响应快？
   - 追问：同步阶段只做了哪些操作？
   - 追问：异步落库是否会让用户感知延迟？

6. Redis Lua 脚本会成为瓶颈吗？
   - 追问：单 Redis 实例的 QPS 上限如何估算？
   - 追问：热点 key 如何分片？

7. MySQL 写入如何优化？
   - 追问：批量消费批量写入是否可行？
   - 追问：索引过多对写入有什么影响？

8. MQ 消费速度如何提升？
   - 追问：增加消费者实例、增加消费线程、优化数据库写入分别有什么效果？
   - 追问：同一个 ConsumerGroup 的并发受什么限制？

9. 接口限流参数如何确定？
   - 追问：根据系统容量反推，还是根据业务规则配置？
   - 追问：限流过严或过松会怎样？

10. 缓存命中率如何提升？
    - 追问：预热、TTL、热点发现、主动失效分别怎么做？

11. JVM 层面如何优化？
    - 追问：线程池、连接池、GC、对象序列化分别可能影响什么？

12. 数据库连接池和 Redis 连接池如何设置？
    - 追问：连接数越大越好吗？
    - 追问：连接池耗尽会如何表现？

13. 如何做容量评估？
    - 追问：如果活动预计 10 万用户同时抢 1000 件商品，要估算哪些指标？

14. 如何做降级？
    - 追问：关闭非核心功能、返回排队中、静态化页面分别适合什么场景？

15. 如果线上 P99 突然升高，你怎么排查？
    - 追问：先看日志、监控、链路追踪、慢查询还是 MQ 堆积？

### 8. 安全、风控与接口设计视角

1. 用户登录态是如何保存的？
   - 追问：Token 存 Redis 的 key 和 TTL 如何设计？
   - 追问：RefreshToken 和 AccessToken 有什么区别？

2. 秒杀接口如何防刷？
   - 追问：限流之外还有哪些手段？
   - 追问：接口地址隐藏是否有必要？

3. 如何防止用户伪造 userId 下单？
   - 追问：为什么从 UserHolder 获取用户，而不是相信前端参数？
   - 追问：Token 被盗怎么办？

4. 如何防止刷短信验证码？
   - 追问：验证码发送接口如何限流？
   - 追问：IP、手机号、设备维度如何组合？

5. 商品支付接口有哪些安全校验？
   - 追问：为什么要校验订单属于当前用户？
   - 追问：实际支付回调如何验签？

6. 如何防止越权访问订单？
   - 追问：查询、支付、取消订单都要校验什么？

7. 限流使用 X-Forwarded-For 有什么风险？
   - 追问：如何在网关层清洗请求头？
   - 追问：可信代理列表怎么配置？

8. 如何防止参数篡改？
   - 追问：商品 id、活动 id、价格是否应该以前端传入为准？
   - 追问：价格应该从哪里读取？

9. 如何防止重复提交？
   - 追问：前端按钮置灰、接口幂等 token、业务幂等分别解决什么？

10. 如何处理恶意爬虫访问店铺和笔记？
    - 追问：IP 限流、User-Agent、行为分析、黑名单怎么做？

11. Redis key 是否会泄露敏感信息？
    - 追问：key 里直接放 userId 是否可接受？
    - 追问：日志里打印用户数据要注意什么？

12. MQ 消息是否可能包含敏感信息？
    - 追问：消息体里应该避免放哪些内容？
    - 追问：消息日志如何脱敏？

13. 上传接口有哪些风险？
    - 追问：文件类型、大小、路径穿越、恶意脚本如何防？

14. 如何防止 SQL 注入？
    - 追问：MyBatis-Plus 条件构造器安全吗？
    - 追问：last() 和拼接 ORDER BY FIELD 有什么风险？

15. 生产环境配置如何保护？
    - 追问：数据库密码、Redis 密码、MQ 地址是否应该写在 yaml 里？
    - 追问：如何使用环境变量或配置中心？

### 9. 运维、监控与排障视角

1. 这个项目如何启动本地环境？
   - 追问：Docker Compose 里有哪些服务？
   - 追问：端口为什么不是默认的 3306、6379？

2. 应用启动失败你怎么排查？
   - 追问：数据库连接、Redis 连接、RocketMQ NameServer、Canal 配置分别怎么确认？

3. Redis 连接失败会影响哪些功能？
   - 追问：登录、缓存、秒杀、限流、点赞是否都会受影响？
   - 追问：哪些功能可以降级？

4. RocketMQ 连接失败会影响哪些功能？
   - 追问：秒杀下单、延迟关单、Canal 缓存同步分别会怎样？

5. Canal 不工作如何排查？
   - 追问：MySQL binlog 是否开启？Canal 用户权限是否正确？
   - 追问：RocketMQ Topic 是否收到消息？

6. 如何观察 MQ 堆积？
   - 追问：使用 RocketMQ Console 还是命令行？
   - 追问：堆积后先扩容还是先查慢消费？

7. 如何排查缓存不一致？
   - 追问：查数据库、查 Redis key、查 Canal 消费日志、查 MQ 重试日志的顺序是什么？

8. 如何排查超卖？
   - 追问：看 Redis 库存、MySQL 库存、订单数、重复订单分别怎么查？

9. 如何排查重复下单？
   - 追问：查 Redis Set、查订单表唯一性、查 MQ 重复消费日志？

10. 如何排查限流误杀？
    - 追问：查看生成的限流 key、ZSet/Hash 内容、客户端 IP 是否正确？

11. 生产环境需要加哪些监控？
    - 追问：接口 QPS、响应时间、错误率、Redis 命中率、MQ 积压、数据库慢 SQL？

12. 需要哪些告警？
    - 追问：秒杀失败率升高、MQ 堆积、Redis 内存过高、Canal 消费失败、死信消息？

13. 日志应该怎么打？
    - 追问：订单 id、用户 id、商品 id、消息 id 是否要作为 trace 信息？
    - 追问：高并发下日志过多会不会影响性能？

14. 如何做链路追踪？
    - 追问：一个秒杀请求跨 HTTP、Redis、MQ、MySQL 如何串起来？

15. 线上事故后如何恢复？
    - 追问：库存错了、订单漏了、缓存脏了、消息积压了分别怎么处理？

### 10. 开放式反问与扩展视角

1. 如果让你把这个项目做到生产可用，你最先补哪三件事？
   - 追问：监控告警、补偿对账、唯一索引、配置中心怎么排序？

2. 如果秒杀活动规模扩大 10 倍，你会怎么改？
   - 追问：限流、库存分片、MQ 扩容、数据库分库分表分别怎么做？

3. 如果用户要求“下单结果必须立刻准确返回”，你会怎么改？
   - 追问：同步落库是否可行？
   - 追问：排队中状态如何设计？

4. 如果业务要求用户取消订单后可以再次秒杀，怎么改一人一单逻辑？
   - 追问：Redis Set 中用户 id 什么时候移除？
   - 追问：数据库唯一索引怎么设计？

5. 如果要做优惠券和普通商品统一秒杀，如何抽象？
   - 追问：库存 key、订单表、业务类型、MQ Topic 如何统一或拆分？

6. 如果 Redis 里库存和数据库库存长期不一致，你怎么定位根因？
   - 追问：从 Redis 预扣、MQ 发送、消费落库、关单回滚哪一步查？

7. 如果要接入真实支付，你需要补哪些能力？
   - 追问：支付单、回调验签、幂等、退款、对账如何设计？

8. 如果要做多端登录和踢人下线，登录模块怎么改？
   - 追问：AccessToken、RefreshToken、设备 id 如何设计？

9. 如果要做搜索店铺和笔记，你会用 MySQL 还是 Elasticsearch？
   - 追问：数据同步、分词、排序、实时性如何考虑？

10. 如果要做推荐系统，这个项目有哪些数据可以利用？
    - 追问：浏览、点赞、关注、下单、地理位置如何建特征？

11. 如果要做商家后台，缓存一致性会变复杂吗？
    - 追问：商家频繁改价、改库存、上下架如何同步到 Redis？

12. 如果你发现一个设计不够严谨，面试时应该怎么讲？
    - 追问：如何坦诚说明 demo 简化点，同时给出生产级改造方案？

13. 如果面试官问“你这个项目是不是烂大街”，你怎么回答？
    - 追问：你做了哪些差异化改造？
    - 追问：你能讲出哪些底层原理和工程权衡？

14. 如果面试官让你现场写 Lua 秒杀脚本，你能写出哪些关键逻辑？
    - 追问：库存判断、一人一单、扣库存、记录用户、返回码怎么写？

15. 如果面试官让你现场设计限流注解，你怎么写接口？
    - 追问：注解字段、AOP 拦截、Redis key、Lua 脚本怎么串起来？

## 三、高频连环追问路径

### 路径 1：秒杀下单

1. 秒杀为什么不用数据库直接扣库存？
2. Redis 预扣库存如何防止超卖？
3. Lua 为什么能保证原子性？
4. Redis 扣成功但 MQ 失败怎么办？
5. MQ 重复消费怎么办？
6. 数据库层如何兜底一人一单？
7. 订单创建成功后为什么要发延迟关单消息？
8. 支付和关单并发怎么保证状态正确？
9. 关单回滚库存如何保证幂等？
10. 如果这些步骤中任何一步宕机，怎么恢复？

### 路径 2：缓存一致性

1. 为什么要缓存店铺数据？
2. 缓存穿透、击穿、雪崩分别是什么？
3. 你用了哪些解决方案？
4. 更新数据库后为什么删除缓存？
5. 删除缓存失败怎么办？
6. Canal 如何监听数据库变更？
7. Canal 消息重复或延迟怎么办？
8. 为什么说最终一致性可以接受？
9. 如何监控缓存命中率和不一致？
10. 如果线上脏缓存影响用户，如何快速处理？

### 路径 3：限流防刷

1. 为什么需要限流？
2. 为什么用 AOP + 注解实现？
3. 限流 key 怎么设计？
4. GLOBAL、IP、USER 三个维度如何组合？
5. 滑动窗口算法怎么实现？
6. 为什么用 ZSet？
7. 令牌桶怎么实现？
8. 为什么用 Lua？
9. Redis 挂了怎么办？
10. 如何防止 X-Forwarded-For 被伪造？

### 路径 4：MQ 可靠性

1. 为什么引入 MQ？
2. MQ 会带来哪些新问题？
3. 消息丢失怎么防？
4. 消息重复怎么防？
5. 消息积压怎么处理？
6. 消费失败怎么重试？
7. 死信消息怎么处理？
8. 延迟消息有什么限制？
9. 事务消息能不能用？
10. 如何监控 MQ？

## 四、建议优先背熟的 20 个问题

1. 秒杀完整链路是什么？
2. Redis + Lua 如何防止超卖和重复下单？
3. Redis 预扣成功但 MQ 发送失败怎么处理？
4. MQ 重复消费如何保证幂等？
5. MySQL 层如何兜底库存和一人一单？
6. 为什么使用 RocketMQ 做异步下单？
7. 延迟关单如何实现？
8. 支付和关单并发如何处理？
9. 关单库存回滚如何保证幂等？
10. 缓存穿透、击穿、雪崩分别怎么解决？
11. Cache Aside 为什么通常是更新数据库后删除缓存？
12. Canal 如何保障缓存最终一致性？
13. 限流注解的实现原理是什么？
14. 滑动窗口和令牌桶有什么区别？
15. Redis ZSet 点赞榜如何实现？
16. Redis 全局 ID 生成器如何设计？
17. Redis 分布式锁有哪些坑？
18. 本机 TPS 2000 是怎么测出来的？
19. 项目里有哪些生产环境需要补齐的点？
20. 如果让你把项目扩展到 10 倍流量，你怎么改？
